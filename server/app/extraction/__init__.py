"""Rolling extraction: one cheap incremental engine that turns the live meeting
transcript into a short working artifact (context + open tasks + preference
candidates) for the wake-word brain, the dashboard, and the end-of-meeting batch
runner. Long-term memory (team preferences, transcript archive) lives alongside
in the Obsidian writer."""
